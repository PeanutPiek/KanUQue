function [H, E_] = entropie(p_)
    E_ = zeros(N, 1);
    for u = 1:1:
        % Catch NaN
        if p_(u) == 0
            % Set to zero instead of NaN
            E_(u) = 0;
        else
            E_(u) = p_(u) * log2(1/p_(u));
        end
    end
    H = sum(E_);
end